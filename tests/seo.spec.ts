import { test } from './../utils/fixtures/myfixture'


test.describe('SEO tests', ()=>{

    test('basic seo test', async({ login })=>{

        const page = await login.getPage()
        //continue with rest of the scripts
        

    })


})